1. 学习前端开发的难点

个人感受：
   需要适配不同的客户端
   样式的调节

需求变化快
性能优化

2. 实习中遇到的“难点”
  换成说在开发过程中比较花时间的地方——*弄清楚组件之间的数据是如何传递的*，也就是数据从哪里获得，要怎么传递到目标组件。
  
  * 添加一个数据时，如果这个数据*会被很多组件用到*，在子组件中生成，且组件之间的关系比较复杂，则*考虑使用状态管理*。
    > 例如树形表格中，每个节点的ID不止在树形表格内要被用到，还有其他位于同一个祖先下的组件，避免跨越多个层级传递，可以直接用状态管理。
  * 对于已有的数据要定位其中的数据流，则通过配合一些调试的手段来定位。

